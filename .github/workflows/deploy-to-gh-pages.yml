name: Build and Deploy Spotlight to GitHub Pages
on:
  push:
    branches: [ main ]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install and Build Spotlight
        run: |
          cd run_motr_in_magpie/spotlight
          npm install
          npm run build

      - name: Add .nojekyll for GitHub Pages
        run: touch run_motr_in_magpie/spotlight/dist/.nojekyll

      - name: Deploy Spotlight to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: run_motr_in_magpie/spotlight/dist
          destination_dir: spotlight
          keep_files: true
